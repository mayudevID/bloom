# Uncomment this line to define a global platform for your project
platform :ios, '15.0'

# CocoaPods analytics sends network stats synchronously affecting flutter build latency.
ENV['COCOAPODS_DISABLE_STATS'] = 'true'

project 'Runner', {
  'Debug' => :debug,
  'Profile' => :release,
  'Release' => :release,
}

def flutter_root
  generated_xcode_build_settings_path = File.expand_path(File.join('..', 'Flutter', 'Generated.xcconfig'), __FILE__)
  unless File.exist?(generated_xcode_build_settings_path)
    raise "#{generated_xcode_build_settings_path} must exist. If you're running pod install manually, make sure flutter pub get is executed first"
  end

  File.foreach(generated_xcode_build_settings_path) do |line|
    matches = line.match(/FLUTTER_ROOT\=(.*)/)
    return matches[1].strip if matches
  end
  raise "FLUTTER_ROOT not found in #{generated_xcode_build_settings_path}. Try deleting Generated.xcconfig, then run flutter pub get"
end

require File.expand_path(File.join('packages', 'flutter_tools', 'bin', 'podhelper'), flutter_root)

flutter_ios_podfile_setup

target 'Runner' do
  use_frameworks!
  use_modular_headers!

  flutter_install_all_ios_pods File.dirname(File.realpath(__FILE__))
  # target 'RunnerTests' do
  #   inherit! :search_paths
  # end
end

post_install do |installer|
  installer.pods_project.targets.each do |target|
    flutter_additional_ios_build_settings(target)

    # Swift module interfaces are incompatible with Objective-C bridging headers.
    target.build_configurations.each do |config|
      config.build_settings['BUILD_LIBRARY_FOR_DISTRIBUTION'] = 'NO'
      config.build_settings['SWIFT_EMIT_MODULE_INTERFACE'] = 'NO'
    end
  end

  installer.aggregate_targets.each do |aggregate_target|
    aggregate_target.xcconfigs.each_value do |config_file|
      config_file.attributes['BUILD_LIBRARY_FOR_DISTRIBUTION'] = 'NO'
      config_file.attributes['SWIFT_EMIT_MODULE_INTERFACE'] = 'NO'
    end
  end

  # Ensure generated Pods-Runner xcconfig files keep this disabled.
  [
    'Pods-Runner.debug.xcconfig',
    'Pods-Runner.release.xcconfig',
    'Pods-Runner.profile.xcconfig',
  ].each do |xcconfig_name|
    xcconfig_path = File.join(__dir__, 'Pods', 'Target Support Files', 'Pods-Runner', xcconfig_name)
    next unless File.exist?(xcconfig_path)

    content = File.read(xcconfig_path)
    content = content.gsub(/^BUILD_LIBRARY_FOR_DISTRIBUTION\s*=\s*YES$/, 'BUILD_LIBRARY_FOR_DISTRIBUTION = NO')
    content = content.gsub(/^SWIFT_EMIT_MODULE_INTERFACE\s*=\s*YES$/, 'SWIFT_EMIT_MODULE_INTERFACE = NO')
    File.write(xcconfig_path, content)
  end
end
